name: Hello

on:
  issues:
   types:
     - opened


jobs:
  newWorld: 
    runs-on: ubuntu-latest

    steps:
      - name: apple
        run: echo '${{ toJSON(github.event) }}' | jq 
      - name: number
        run: echo '${{ github.event.issue.number }}'
