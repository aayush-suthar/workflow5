name: Hello

on:
  issues:
   types:
     - opened
  workflow_dispatch:

jobs:
  newWorld: 
    runs-on: ubuntu-latest

    steps:
      - name: apple
        run: echo '${{ toJSON(github.event) }}' | jq 
