name: hi
on:
  push:
      branches:
        - 'main'
        
jobs:
   build:
       runs-on: ubuntu-latest
       
       steps:
     
       - name: first
         run: echo ${{ toJSON(github.event) }} | jq 
       
