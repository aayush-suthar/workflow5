name: Hello

on:
  issues:
   types:
     - opened

jobs:
  newWorld: 
    runs-on: ubuntu-latest

    steps:
      - name: apple
        run: echo "Hello World"
      - name: banana
        run: |
             echo "Nice Raaa"
             echo "Why raaaa"
